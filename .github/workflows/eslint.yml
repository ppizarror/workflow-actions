name: ESLint

on:
  workflow_call:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install modules
        run: yarn

      - name: Run
        run: npm run lint